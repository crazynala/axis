export const buildContactFindConfig = () => [];

export const deriveContactSemanticKeys = () => new Set<string>();

export const contactFind = {
  buildConfig: buildContactFindConfig,
  deriveSemanticKeys: deriveContactSemanticKeys,
};
